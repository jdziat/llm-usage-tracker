# Build artifacts
/llmut
/llmut.exe
/dist/

# Generated reports (README examples write these to the repo root)
/usage.html
/usage.csv
/usage.json

# Go workspace — dev-only, must never be committed or it would pull the
# desktop module's Wails dependency graph into the stdlib-only root build.
/go.work
/go.work.sum

# Desktop (Wails) build + frontend artifacts
/desktop/desktop
/desktop/build/bin/
/desktop/frontend/node_modules/
/desktop/frontend/dist/
/desktop/frontend/wailsjs/

# Tool caches and agent-dispatch artifacts
/.pytest_cache/
/.codex-dispatch/

# Editor / OS cruft
.idea/
.vscode/
*.swp
.DS_Store
